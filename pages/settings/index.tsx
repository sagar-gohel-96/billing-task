import React from "react";

const Settings = () => {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Settings
    </div>
  );
};

export default Settings;
