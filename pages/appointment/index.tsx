import React from 'react';

const Appointment = () => {
  return (
    <div
      style={{
        width: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
      }}>
      Appointment
    </div>
  );
};

export default Appointment;
